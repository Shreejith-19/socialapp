package com.example.socialapp.enums;

/**
 * Appeal Type Enumeration.
 * Distinguishes between different types of appeals in the system.
 */
public enum AppealType {
    /**
     * Appeal against a user ban.
     */
    BAN,

    /**
     * Appeal for a removed/rejected post.
     */
    POST
}
